# Copy these variables to DigitalOcean App Platform (Production environment).
# Update placeholder values like <your-app-domain> before importing.

NODE_ENV=production
PORT=8080

DATABASE_URL="mysql://username:password@your-database-host:25060/your-database-name?sslmode=require"

# Domain assigned by App Platform, e.g. https://fuelsense-app.ondigitalocean.app
FRONTEND_URL="https://<your-app-domain>"
ALLOWED_ORIGINS="https://<your-app-domain>"

# Frontend build needs this to call the API (same domain when bundled)
VITE_BACKEND_URL="https://<your-app-domain>"

# Optional: uncomment if using custom static directory override
# FRONTEND_DIST_PATH="../../frontend/dist"
